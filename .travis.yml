language: python
sudo: false

services:
  - mongodb
  - redis-server
  - rabbitmq

addons:
  apt:
    packages:
    - ldap-utils
    - apache2

python:
  - "2.7"

# command to install dependencies
install:
  - "pip install -r requirements.txt"

# command to run tests
#script: nosetests