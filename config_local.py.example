#config_local.py
environment = 'ENVIRONMENT'

# LDAP Service account credentials.
ldap_user = 'LDAP_USER'
ldap_pw = 'LDAP_PASSWORD'

# User that orchestration runs as.
ssh_user = 'SSH_USER'

# TODO: Figure out what this does.
allowed_users = ['USER']

# If an environment needs a different url for the API.
api_server = 'URL'

# Google Search Appliance credentials
gsa_host = 'HOSTNAME'
gsa_user = 'USER'
gsa_pw = 'PASSWORD'
